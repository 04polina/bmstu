lst_numbers = list(map(int, input().split()))

if len(lst_numbers) < 4:
    print("Вы ввели менее 4 чисел!")
else:
    lst_no_extremum = list()
    for num in lst_numbers:
        if num != max(lst_numbers) and num != min(lst_numbers):
            lst_no_extremum.append(num)
    print(lst_no_extremum)
